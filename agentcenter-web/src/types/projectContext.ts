export interface ProjectContextSelection {
  project: string
  space: string
  iteration: string
}

export interface ProjectContextOptions {
  projects: string[]
  spaces: string[]
  iterations: string[]
}
