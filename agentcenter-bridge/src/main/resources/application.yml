server:
  port: 8080

spring:
  devtools:
    restart:
      enabled: false
  datasource:
    driver-class-name: org.sqlite.JDBC
    url: jdbc:sqlite:file:./data/agentcenter.db?busy_timeout=10000&journal_mode=WAL&synchronous=NORMAL
    hikari:
      pool-name: AgentCenterSqlitePool
      maximum-pool-size: 1
      minimum-idle: 1
      connection-timeout: 30000
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true
    clean-disabled: true

mybatis:
  mapper-locations: classpath:mapper/**/*.xml
  type-aliases-package: com.agentcenter.bridge.domain
  configuration:
    map-underscore-to-camel-case: true

agentcenter:
  runtime:
    opencode:
      serve:
        enabled: true
        hostname: 127.0.0.1
        port: 4097
        working-directory: ${AGENTCENTER_RUNTIME_WORKSPACE:${user.dir}/runtime-workspace}
        command: opencode
        agent: build
      command: opencode
      model: ""
      timeout-seconds: 1800
