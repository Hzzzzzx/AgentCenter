<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.agentcenter.bridge.infrastructure.persistence.mapper.RuntimeResourceAuditMapper">

    <select id="findByProjectIdAndResourceId" resultType="com.agentcenter.bridge.infrastructure.persistence.entity.RuntimeResourceAuditEntity">
        SELECT id, project_id, resource_type, resource_id, action, status,
               summary, detail_json, created_by, created_at
        FROM runtime_resource_audit
        WHERE project_id = #{projectId}
          AND resource_type = #{resourceType}
          AND resource_id = #{resourceId}
        ORDER BY created_at DESC
    </select>

    <insert id="insert">
        INSERT INTO runtime_resource_audit (id, project_id, resource_type, resource_id, action, status,
                                            summary, detail_json, created_by, created_at)
        VALUES (#{id}, #{projectId}, #{resourceType}, #{resourceId}, #{action}, #{status},
                #{summary}, #{detailJson}, #{createdBy}, #{createdAt})
    </insert>

</mapper>
