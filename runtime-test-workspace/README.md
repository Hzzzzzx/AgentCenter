# AgentCenter Runtime Test Workspace

This directory is a small isolated workspace for testing AgentCenter runtime routing.

Use it to verify:

- Work item conversations run in the intended working directory.
- Runtime skills can read workspace-local context.
- MCP servers can expose workspace-local tools.

Expected marker:

```text
AGENTCENTER_RUNTIME_TEST_WORKSPACE=runtime-test-workspace
```

