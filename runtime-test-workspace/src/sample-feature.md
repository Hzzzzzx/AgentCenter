# Sample Feature

## Feature

Runtime workspace routing verification.

## Expected Agent Behavior

When a task runs in this workspace, the agent should mention:

- The workspace marker `AGENTCENTER_RUNTIME_TEST_WORKSPACE`.
- The sample feature file `src/sample-feature.md`.
- The test MCP server name `agentcenter-test-workspace`.

